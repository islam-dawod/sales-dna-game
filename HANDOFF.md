# SALES DNA — ملخّص التسليم / HANDOFF

> الصق هذا الملف (أو محتواه) في أول رسالة من المحادثة الجديدة.

## 1 · ما هو المشروع

نظام تقييم مبيعات على شكل لعبة (Vanilla JS بلا أدوات بناء) + واجهة برمجية PHP/MySQL.
ثلاثة مسارات: **موظف حالي** · **مرشّح جديد** · **مدير**.
اللغتان: **عربي (RTL) وإنجليزي (LTR)** — زر التبديل في الشريط العلوي. العبرية أُزيلت بالكامل.

## 2 · الروابط

| | الرابط | الوضع |
|---|---|---|
| الإنتاج | https://driftx.co/dna/ | Apache + Plesk · المجلد الفعلي `/driftx.co/new/dna` |
| نسخة التطوير | https://islam-dawod.github.io/sales-dna-game/ | تُنشر تلقائياً مع كل push |
| المستودع | https://github.com/islam-dawod/sales-dna-game | عام · فرع `main` |

## 3 · حالة الـ Backend — يعمل ✅

شُغِّل بتاريخ 2026-08-20 والتثبيت مغلق.

```
GET https://driftx.co/dna/api/index.php?r=health
→ {"ok":true,"php":"8.2.30","db":true,"employees":24,
   "with_code":24,"manager_pass_set":true}
```

البيئة: PHP 8.2.30 · MariaDB 10.6.23 · قاعدة `DNA` · مستخدم `dna_app` محصور بها
واتصالات محلية فقط. `allow_install = false`، و`setup.php` و`install.php` محذوفان من السيرفر.

**فُحِص وتأكّد:** الاتصال · 24 موظفاً بأكواد · كلمة سر المدير · رفض الكود الخاطئ ·
رفض كلمة سر المدير الخاطئة · الحظر بعد 10 محاولات لمدة 15 دقيقة · منع `state` بلا تصريح ·
تسجيل مرشّح فعلي في القاعدة.

**درس يستحق الحفظ — أهدر ساعات:** الخطأ `1045 Access denied for user 'driftx'@'localhost'`
لم يكن كلمة مرور خاطئة إطلاقاً. Plesk أنشأ المستخدم بـ «Allow remote connections from any host»
أي `driftx@%`، وMariaDB يطابق السجل الأخص `''@'localhost'` (مستخدم مجهول) أولاً فيرفض كل
اتصال محلي. إعادة إنشائه بـ **Allow local connections only** حلّها فوراً.

مصيدتان أخريان في نفس الجلسة: Plesk يعرض المضيف `localhost:3306` فتُلصق كاملة في خانة المضيف
(المعالج يفصلها الآن)، و`config.sample.php` يحتوي `db_name => 'dna'` وهي قاعدة مختلفة عن `DNA`
على لينكس.

للتشخيص: `db_diag()` في `api/lib.php` يُرفق رسالة PDO الحقيقية والقيم المقروءة من `config.php`
مع أي فشل اتصال — مقيّد بـ `allow_install` فيصمت تلقائياً بعد الإغلاق. بدونه كان الـ API
يقول `db_connect_failed` فقط بلا أي تفصيل.

## 4 · البنية

```
index.html
api/  index.php (18 مساراً) · lib.php (PDO + المخطط + الجلسات + الحظر)
      setup.php  معالج التثبيت: يكتب config.php ثم يحذف نفسه   ← ابدأ من هنا
      seed.php   سجل الـ24 كبصمات + الزرع (مشترك بين setup و install)
      install.php (المسار اليدوي) · config.sample.php · .htaccess (يمنع قراءة config/lib/seed عبر HTTP)
      [config.php و.setup-owner مستثنيان من git]
assets/js/
  api.js          يفحص وجود الـ API عند الإقلاع → وضع سيرفر أو وضع محلي
  art.js          كل الرسومات SVG: البطل (4 وضعيات) · 5 عملاء · مدرّب · مدينة · شعارات
  questions.js    115 سؤالاً للموظفين · 8 مناطق · 12 صفة · حقل aud يمنع أسئلة معيّنة عن الموظفين
  candidate25.js  نموذج المرشّح المقفل: 25 سؤالاً · 5 مستويات × 5 · تعيين متعدد الصفات
  focus.js        تحدي التركيز: Spot the Difference (إحداثيات نسبية) + Quick Scan
  store.js        الحالة · سجل الـ24 موظفاً كبصمات · hydrate من السيرفر
  engine.js       DNA · التشابه · الفروقات · جودة الأسئلة · الثقة · التنبؤ
  ui.js           i18n عربي/إنجليزي (ar/en) · Radar/Bars/Ring · تشفير كلمات السر
  game.js         مسار اللعبة (موظف 36 · موظف مقارن 22 · مرشّح 25) + إرسال النتائج
  manager.js      الكونسول: 10 تبويبات
  app.js          Router وشاشات الدخول
assets/css/style.css
```

## 5 · القواعد التي يجب عدم كسرها

1. **الموظف الحالي لا يُسأل** عن الدراسة، عمل آخر، الانتقال، أو خطط مستقبلية (`aud:'cand'`؛ مُتحقق على 300 توليد خطة: صفر تسريب).
2. **المرشّح لا يرى أي نتيجة** — فقط «CHALLENGE COMPLETE».
3. **XP للتقدّم فقط** (50/سؤال + 500/منطقة) ولا يرتبط بجودة الإجابة.
4. **لا فلترة أوتوماتيكية**: 🟢 متابعة / 🟡 مراجعة / 🔴 تطابق منخفض — والقرار للمدير.
5. **تحدي التركيز وزنه 0%** ولا يدخل نسبة التطابق حتى تثبت البيانات ارتباطه.
6. **الأسرار كبصمات فقط**: لا كلمات سر ولا أكواد في أي ملف يُنشر · `config.php` خارج git.
7. الأداء الفعلي و DNA و MATCH ثلاثة أرقام منفصلة لا تُدمج.
8. **الترجمة كاملة لا جزئية**: أي سؤال جديد يحتاج `q_en` وكل خيار يحتاج `t_en`، وأي مدخل في
   `DICT` يحتاج `en`. العربي هو الاحتياطي عند غياب الإنجليزي، فالنقص لا يُسبّب خطأً — لكنه
   يُظهر عربياً داخل واجهة إنجليزية. النصوص تُقرأ عبر `UI.T` · `UI.qt(obj,'q')` · `UI.nm(obj)`.
9. **سؤال بلا إجابة ليس إجابة خاطئة**: عند انتهاء الوقت يُسجَّل `unanswered:true` و`opt:null`،
   ويُستثنى من البسط والمقام معاً في `NC.score()` و`Engine.dna()`. مُتحقَّق: نفس النتيجة تماماً
   ونفس الأبعاد سواء غاب السؤال أو حُفظ كـ«بلا إجابة» — الفرق يظهر في `completeness` وحده.
10. **السرعة بيانات لا حكم**: `avgMs` و`completeness` و`match` أرقام منفصلة. لا يُفترض أن
    الأسرع أفضل ولا الأبطأ أضعف حتى تُثبت بيانات الشركة أن الأقوياء يختلفون في سرعة القرار
    (نفس منطق وزن التركيز 0%).

## 5.1 · المؤقّت (Timed Assessment)

مؤقّت لكل **مرحلة** لا للاختبار كله. الافتراضي 3:00، ويضبطه المدير من الإعدادات
(2:00 / 2:30 / 3:00 / 4:00 / 5:00) أو يعطّله كلياً — `settings.timerEnabled` و`settings.levelSeconds`.

- الساعة تبدأ عند زر «ابدأ المرحلة» فقط، وتظهر في `.hud` (وهو `position:fixed` فيبقى ظاهراً على الجوال).
- تحذير بصري عند دقيقة (`t-warn`) وعند 30 ثانية (`t-hot`)، ويُعطَّل مع `prefers-reduced-motion`.
- انتهاء الوقت: تُسجَّل الأسئلة المتبقية «بلا إجابة» → شاشة «انتهى الوقت» → انتقال تلقائي بعد 1.6 ثانية.
  **بلا نافذة تأكيد وبلا تمديد** — هذا مقصود.
- كل إجابة تُكتب فوراً في `localStorage` تحت `sdna_run_v1` مع `ms` (زمن الإجابة).
- **مقاومة التحديث:** الـ deadline طابع زمني مطلق محفوظ مع الجولة، فإعادة تحميل الصفحة تستأنف
  نفس المرحلة بالوقت المتبقي فعلاً. مُتحقَّق: 0:54 قبل التحديث و0:54 بعده.
- تقرير المدير: `Engine.timingStats(payload)` → اكتمال · بلا إجابة · متوسط/أسرع/أبطأ زمن ·
  زمن كل مرحلة مقابل حدّها · عدد المراحل التي انتهى وقتها.
- التخزين: عمود `assessments.timing` (JSON) يُضاف عبر `add_column()` — ترحيل إضافي آمن التكرار
  لأن `CREATE TABLE IF NOT EXISTS` لا يعدّل جدولاً قائماً.

**الثقب المعروف:** المقاومة على العميل لا السيرفر. الموظف يعود بنفس الـ id (يدخل بكوده) فيستأنف
صحيحاً، أما المرشّح فيحصل على id جديد عند إعادة التسجيل فيبدأ من الصفر بساعات جديدة. الإنفاذ
الكامل يحتاج مساراً في الـ API يفتح المرحلة ويخزّن `levelStartedAt` على السيرفر.

## 5.2 · V4 — ما نُفِّذ من ملف المراجعة (77 بنداً)

**تنبيه على أساس الوثيقة:** بُنيت على قراءة سطر `demo build — data is stored locally`
الذي بقي في الـ footer بعد تشغيل الـ Backend وصار كاذباً. فبنود `PRIORITY 0` رقم 1 و2
(قاعدة مركزية · API) كانت موجودة قبل الوثيقة. السطر أُصلح ويعرض الوضع الفعلي الآن.

**نُفِّذ:** 1، 2، 4 (محلياً فورياً)، 5، 7، 9، 10، 13، 14، 22–25، 26–28 (جزئياً)، 31، 32،
34، 35، 40، 41، 42–57، 58–62، 68، 69، 73، 74، 77.

**لا ينطبق:** 19، 20، 21، 30 — **لا توجد صور نقطية في المشروع إطلاقاً**؛ كل الرسوم SVG
مضمّن، فتحويل AVIF/WebP و`srcset` واستبدال GIF وتحميل صور Spot-the-Difference مسبقاً
كلها بلا هدف تعمل عليه.

**متبقٍّ (يحتاج عملاً على السيرفر):**
- **3 · 8 · 15** جلسة تقييم على السيرفر: `assessment_session_id` وحالة `current_question`
  كمصدر حقيقة، وشاشة Recovery للمدير (Resume / Recalculate / Close). المقاومة الحالية
  على العميل: الموظف يستأنف بنفس الـ id، والمرشّح يبدأ من جديد بـ id جديد.
- **4 (النصف الثاني)** حفظ كل إجابة على السيرفر لحظة الضغط. حالياً تُكتب فوراً في
  `localStorage` وتُرفع كاملة عند النهاية مع 3 محاولات وزر إعادة.
- **26 (بقية التقسيم)** فصل `focus.js` و`engine.js`. المنجز: `manager.js` (105KB = 23%)
  لم تعد تُحمَّل للمرشّح.
- **36–39** إعادة تسمية الجداول للأسماء المقترحة — الموجود مكافئ وظيفياً.
- **63–67** دقة التنبؤ موجودة في `predictionValidation()` لكن بلا شاشة مخصّصة للفشل
  (`MODEL MISS`) وتحليل أسبابه.
- **76** اختبارات آلية.

**قاعدة V4 المضافة:** الاختيار بالأرقام لا بالتصنيف. `hittersDNA()` تقسم بالتحقيق الفعلي،
وفارق أقل من 5 نقاط يُصنَّف «لا يفرّق» ولا يُبنى عليه قرار (حالة Focus 81/79 في الوثيقة).

## 6 · نموذج المرشّح (25 سؤالاً)

الأوزان: يعد 25% · إصرار 20% · انضباط 10% + التزام 10% · مسؤولية 15% · تعلّم 10% · **اتساق 10%**
(المستوى الخامس أسئلة تقاطع تُقارن بالبروفايل المبني في المستويات 1–4).
زر «معايرة الأوزان من الموظفين الأقوياء» يعيد توزيع الـ90% حسب ما يفرّق فعلياً.

## 7 · الموظفون

24 موظفاً بلا تصنيف وبلا أرقام (بانتظار البيانات الحقيقية): **مصر 16 · رام الله 2 · فلسطين 6**.
لكل موظف كود خاص بصيغة `ABCD-1234` مخزّن كبصمة. الأكواد سُلّمت في المحادثة وفي
ملف `SALES-DNA-Employees.xlsx` (3 صفحات: الأكواد · بيانات الأداء · السجل الشهري).
**الأكواد وكلمة سر المدير غير موجودة في هذا الملف ولا في المستودع.**

## 8 · الخطوات المقترحة بعد تشغيل الـ Backend

1. تمرير «التحدي المقارن» (22 موقفاً) على كل الموظفين — يغذّي تحليل جودة الأسئلة.
2. إدخال أرقام المبيعات والسجل الشهري من ملف الإكسل، ثم التصنيف قوي/متوسط/ضعيف.
3. مراجعة Common DNA مقابل Success Differentiators ثم معايرة الأوزان.
4. بدء اختبارات المرشحين ومتابعة 30/90/180 والتحقق من دقة التنبؤ.
5. لاحقاً: قفل كونسول المدير على مستوى السيرفر (مجلد محمي بكلمة مرور في Plesk).
